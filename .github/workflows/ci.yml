name: CI Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-and-lint:
    name: Run Linting and Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: "pip"

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run Ruff Linter
        run: |
          ruff check .
          ruff format --check .

      - name: Run Pytest
        env:
          # Dummy secrets for testing in CI without a .env file
          JWT_SECRET_KEY: super-secret-test-key-for-testing
          DATABASE_URL: sqlite:///./test.db
          GROQ_API_KEY: dummy-api-key-for-testing
        run: |
          pytest -v
